This directory contains commands which are used by the corba cli package
config 
  init
  generate
  validate
gleaner
  setup
  batch
  check

nabu What Nabu should do: tika, txtai, object, prefix, prune
  prefix
  prune