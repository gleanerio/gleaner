

- modify the go code to have an "init" call that builds the local/gleaner bucket
and puts in an initial config.json

- need a "extract config" to pull the config file to the PWD

- need a "load config" to load the json config from the PWD


