package utils

import "log"

// ExtractJSONLD pulls JSONLD from an HTML file
func ExtractJSONLD(file []byte) (string, error) {

	log.Printf("I dont' use this..  but maybe I can put the jsonLD functions here and clean the code a bit")

	log.Printf("length %d of html", len(file))

	return "jsonld", nil
}
